/**
 * 앱 전체가 쓰는 색.
 *
 * 미니게임을 여러 사람이 나눠 만들기 때문에 각자 색을 고르면 판마다 화면이 튄다.
 * 한 세션에서 3판이 연속으로 도는 앱이라 톤이 갈라지는 게 바로 보인다.
 * 색을 새로 쓸 일이 생기면 여기에 이름을 붙여 추가한다 — 게임 폴더 안에 하드코딩하지 않는다.
 *
 * `games/__tests__/theme.test.tsx`가 등록된 모든 게임이 이 배경을 쓰는지 검사한다.
 */
export const COLORS = {
  /** 모든 화면의 바탕 */
  bg: '#E9E9ED',
  /** 바탕 위에 얹는 판·카드 */
  surface: '#FFFFFF',
  /** 눌리거나 비활성인 면 */
  surfaceAlt: '#DCDCE4',
  /** 면과 면을 가르는 선 */
  border: '#C7C7D2',

  /** 본문 글씨 */
  text: '#1B1B22',
  /** 라벨처럼 한 단계 죽인 글씨 */
  textMuted: '#63636F',
  /** 힌트처럼 더 죽인 글씨 */
  textFaint: '#93939F',

  /**
   * 점수·강조.
   *
   * 어두운 배경에서 쓰던 노랑(#ffd23f)은 밝은 회색 위에서 글씨로 읽히지 않는다.
   * 같은 계열이되 명도를 낮춘 호박색을 쓴다.
   */
  accent: '#E08A00',
  /** 강조면 위에 얹는 글씨. accent 위에서 대비가 나온다. */
  onAccent: '#FFFFFF',

  /**
   * 브랜드 보라.
   *
   * 앱 화면의 버튼·강조에 쓰이던 색인데 게임에서도 같은 보라를 쓰는 곳이
   * 있어서(tapRush 카운터) 여기로 올린다. theme/colors.ts의 primary가
   * 이 값을 가져간다 — 정의는 한 곳뿐이어야 한다.
   */
  brand: '#A161F7',

  /**
   * 계란과 병아리 (tapRush 부화 연출).
   *
   * 게임 폴더에 하드코딩하지 않는 규칙을 따라 여기에 올린다.
   * 껍데기는 밝은 회색 배경 위에서 흰 판(surface)과 구별돼야 해서
   * 크림 쪽으로 살짝 틀었다.
   */
  shell: '#FAF3E6',
  shellLine: '#C9B892',
  chick: '#FFD84D',
  chickPale: '#FFE894',
  beak: '#F2994A',

  /** 잘한 것 */
  good: '#159F5B',
  /** 틀린 것·남은 시간이 급한 것 */
  bad: '#E03151',
} as const

export type ColorName = keyof typeof COLORS
